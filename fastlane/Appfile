
#----- Android sections -----
json_key_file("./android/gcp_distribute_key.json") # Path to the json secret file - Follow https://docs.fastlane.tools/actions/supply/#setup to get one
package_name("com.yourcompany.myreactnativecicd") # TODO - Add your own app bundle name

#----- IOS section ---------
app_identifier(ENV["APP_IDENTIFIER"]) # The bundle identifier of your app
apple_id(ENV["APPLE_ID"]) # Your Apple email address
itc_team_id(ENV["APP_STORE_TEAM_ID"]) # App Store Connect Team ID
team_id(ENV["DEVELOPER_TEAM_ID"]) # Developer Portal Team ID
#---- match to use main instead of master
